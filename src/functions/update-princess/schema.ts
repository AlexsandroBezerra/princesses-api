export default {
  type: 'object',
  properties: {
    title: { type: 'string' },
    subTitle: { type: 'string' },
    history: { type: 'string' },
    assetUrl: { type: 'string' },
    date: { type: 'string' }
  }
} as const
